import { PublicFrame } from '../components/SiteFrame.jsx'

export default function PlaceholderPage({ title }) {
  return (
    <PublicFrame title={title}>
      <p>Tato část webu je připravená pro další obsah.</p>
    </PublicFrame>
  )
}
